# Hybrid (Windows, Python 3.11): единый файл для запуска гибрида.
# Собран из magnetcursor/requirements.txt + EyeConnect-Ramazan/eyeconnect/requirements.txt,
# дубли убраны, добавлен pytest. Pi-зависимости (requirements-pi.txt) НЕ тянуть.
# Установка: pip install -r requirements-hybrid.txt (из Ramazan/)
# Примечание: face-detection с RetinaFace ставится отдельно из
# EyeConnect-Ramazan/third_party/face-detection/ (на PyPI нет RetinaFace).

# --- magnetcursor/requirements.txt ---
# Только Windows (UI Automation). На других ОС магнит не работает.
uiautomation; sys_platform == 'win32'

# --- EyeConnect-Ramazan/eyeconnect/requirements.txt ---
opencv-python>=4.8
numpy>=1.24,<2.0
scikit-learn>=1.3
pillow>=10.0
mediapipe>=0.10.21
eyetrax>=0.2.2
torch>=2.0
torchvision>=0.15
safetensors>=0.4
huggingface_hub>=0.20
unigaze>=0.1.3
face-alignment>=1.5.0
timm==0.3.2
scikit-image>=0.21
tqdm>=4.60

# --- тесты гибрида ---
pytest
